/**
 * A JavaScript object that holds the data for the selects in the PokeBowl app.
 * This mimics an external data source like a database or API.
 */
export const selectData = {
    "base": ["rice", "mixed-greens"],
    "protein": ["salmon", "tuna"],
    "toppings": ["edamame", "mango"]
};
